# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
server:
  endpoint: {{ .ServiceEndpoints.Server | default "localhost:0" }}
  server-creds:
    tls-mode: {{ .ServiceTLS.Mode }}
    server-name: {{ .ServiceTLS.ServerName }}
    cert-path: {{ .ServiceTLS.CertPath }}
    key-path: {{ .ServiceTLS.KeyPath }}
    ca-cert-paths:
      {{- range .ServiceTLS.CACertPaths }}
      - {{ . }}
      {{- end }}
  keep-alive:
    params:
      time: 300s
      timeout: 600s
    enforcement-policy:
      min-time: 60s
      permit-without-stream: false
monitoring:
  server:
    endpoint: {{ .ServiceEndpoints.Metrics | default "localhost:0" }}

orderer:
  connection:
    endpoints:
      {{- range .Endpoints.Orderer }}
      - {{ .Server }}
      {{- end }}
  channel-id: {{ .ChannelID }}
  consensus-type: BFT
committer:
  client:
    endpoint: {{ .Endpoints.Coordinator.Server }}
    server-creds:
      tls-mode: {{ .ClientsConfig.Coordinator.Mode }}
      server-name: {{ .ClientsConfig.Coordinator.ServerName }}
      cert-path: {{ .ClientsConfig.Coordinator.CertPath }}
      key-path: {{ .ClientsConfig.Coordinator.KeyPath }}
      ca-cert-paths:
        {{- range .ClientsConfig.Coordinator.CACertPaths }}
        - {{ . }}
        {{- end }}

ledger:
  path: {{ .LedgerPath }}

last-committed-block-set-interval: 5s

bootstrap:
  genesis-block-file-path: {{ .ConfigBlockPath }}

logging:
  enabled: {{ .Logging.Enabled }}
  level: {{ .Logging.Level }}
  caller: {{ .Logging.Caller }}
  development: {{ .Logging.Development }}
  output: {{ .Logging.Output }}
  name: sidecar
